VITE_FIREBASE_API_KEY=your-firebase-web-api-key
VITE_GEMINI_MODEL=gemini-2.5-flash

# Optional: Jarvis local Firebase SDK access for repo-scoped read/write scripts.
# Keep these only in local .env.local on Jarvis's machine; never commit real secrets.
TC_EMAIL=jarvis.mac.ai@gmail.com
TC_PASSWORD=your-jarvis-password
TC_CANVAS_ID=main
